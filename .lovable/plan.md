
# خطة تحسين بيانات العملاء ونظام الملاحظات واستيراد Excel

## ملخص التغييرات المطلوبة
1. إضافة بيانات جهة الاتصال المخولة للعميل
2. تعميم نظام الملاحظات ليشمل جميع التبويبات
3. عرض جميع الملاحظات في تبويب البيانات الأساسية
4. إنشاء نموذج Excel وميزة استيراد العملاء

---

## 1. جهة الاتصال المخولة

### التغييرات المطلوبة في قاعدة البيانات
إضافة أعمدة جديدة لجدول `client_organizations`:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `primary_contact_name` | text | اسم جهة الاتصال المخولة |
| `primary_contact_email` | text | بريد جهة الاتصال |
| `primary_contact_phone` | text | جوال جهة الاتصال |
| `use_org_contact_info` | boolean | استخدام نفس بيانات المؤسسة |

### تحديث الواجهة
- إضافة قسم "جهة الاتصال المخولة" في `BasicInfoTab`
- خيار checkbox "استخدام نفس بيانات المؤسسة"
- عند التفعيل: نسخ contact_email و contact_phone واسم المؤسسة تلقائياً

---

## 2. تعميم نظام الملاحظات

### تعديل جدول `subscription_notes` إلى `customer_notes`
تحويل الجدول الحالي إلى جدول ملاحظات موحد:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | uuid | المعرف |
| `organization_id` | uuid | معرف المؤسسة |
| `note_type` | text | نوع الملاحظة (جديد) |
| `note` | text | نص الملاحظة |
| `created_by` | uuid | معرف الموظف |
| `created_at` | timestamp | تاريخ الإنشاء |

### أنواع الملاحظات المتاحة:
```text
general      - ملاحظة عامة
subscription - ملاحظة الاشتراك
sales        - ملاحظة المبيعات
invoices     - ملاحظة الفواتير
hosting      - ملاحظة الاستضافة
delivery     - ملاحظة التنفيذ
tickets      - ملاحظة التذاكر
meetings     - ملاحظة الاجتماعات
```

### إنشاء مكون موحد للملاحظات
```text
CustomerNotesSection
├── Props: organizationId, noteType (optional), showAll
├── يعرض الملاحظات حسب النوع أو جميعها
├── إضافة ملاحظة جديدة مع اختيار النوع
└── حذف الملاحظات
```

---

## 3. عرض الملاحظات في البيانات الأساسية

### تحديث `BasicInfoTab`
- استبدال قسم "الملاحظات الداخلية" الحالي
- عرض جميع الملاحظات مع فلتر حسب النوع
- عرض badge ملون لكل نوع ملاحظة
- إمكانية إضافة ملاحظات من هذا التبويب

### التصميم المقترح:
```text
┌─────────────────────────────────────────────────────┐
│ 📝 الملاحظات                      [+ إضافة ملاحظة] │
├─────────────────────────────────────────────────────┤
│ الكل | اشتراك | مبيعات | فواتير | استضافة | تنفيذ  │
├─────────────────────────────────────────────────────┤
│ [اشتراك] تم التواصل مع العميل بخصوص التجديد        │
│          أحمد محمد - 15 يناير 2026                 │
│                                                     │
│ [مبيعات] العميل مهتم بالترقية للباقة الاحترافية    │
│          سارة أحمد - 10 يناير 2026                 │
└─────────────────────────────────────────────────────┘
```

---

## 4. استيراد العملاء من Excel

### إنشاء نموذج Excel
ملف نموذجي بالأعمدة التالية:
```text
اسم المؤسسة | نوع المؤسسة | البريد الإلكتروني | رقم الهاتف | المدينة | رقم الترخيص | الموقع الإلكتروني | اسم جهة الاتصال | بريد جهة الاتصال | جوال جهة الاتصال | حالة الاشتراك | الباقة
```

### قيم نوع المؤسسة المقبولة:
- جمعية خيرية (charity)
- منظمة غير ربحية (nonprofit)
- مؤسسة (foundation)
- جمعية تعاونية (cooperative)
- أخرى (other)

### واجهة الاستيراد في صفحة العملاء
```text
┌─────────────────────────────────────────────────────┐
│                   استيراد العملاء                    │
├─────────────────────────────────────────────────────┤
│  📥 اسحب ملف Excel هنا أو انقر للاختيار            │
│                                                     │
│  [⬇️ تحميل نموذج Excel]                            │
│                                                     │
│  ─────────────────────────────────────────────────  │
│  بيانات المعاينة (قبل الاستيراد):                  │
│  ┌─────────┬──────────┬─────────────┐              │
│  │ الاسم   │ البريد   │ الحالة      │              │
│  ├─────────┼──────────┼─────────────┤              │
│  │ مؤسسة أ │ a@x.com  │ ✓ جاهز     │              │
│  │ مؤسسة ب │ (فارغ)   │ ✗ خطأ      │              │
│  └─────────┴──────────┴─────────────┘              │
│                                                     │
│  [إلغاء]                    [استيراد X عملاء]      │
└─────────────────────────────────────────────────────┘
```

---

## التفاصيل التقنية

### تعديلات قاعدة البيانات

**Migration 1: إضافة أعمدة جهة الاتصال**
```sql
ALTER TABLE client_organizations ADD COLUMN IF NOT EXISTS 
  primary_contact_name text,
  primary_contact_email text,
  primary_contact_phone text,
  use_org_contact_info boolean DEFAULT false;
```

**Migration 2: تعديل جدول الملاحظات**
```sql
-- إعادة تسمية الجدول
ALTER TABLE subscription_notes RENAME TO customer_notes;

-- إضافة عمود نوع الملاحظة
ALTER TABLE customer_notes ADD COLUMN note_type text DEFAULT 'subscription';

-- تحديث سياسات RLS
```

### الملفات المتأثرة

| الملف | نوع التغيير |
|-------|-------------|
| قاعدة البيانات | Migration جديد |
| `BasicInfoTab.tsx` | تحديث كبير |
| `SubscriptionNotesSection.tsx` | تحويل إلى CustomerNotesSection |
| `SubscriptionTab.tsx` | استخدام المكون الجديد |
| `SalesTab.tsx` | إضافة قسم الملاحظات |
| `InvoicesTab.tsx` | إضافة قسم الملاحظات |
| `HostingTab.tsx` | إضافة قسم الملاحظات |
| `DeliveryTab.tsx` | إضافة قسم الملاحظات |
| `ClientsPage.tsx` | إضافة واجهة الاستيراد |
| `public/templates/clients-template.xlsx` | نموذج Excel جديد |

### مكونات جديدة
- `CustomerNotesSection.tsx` - مكون الملاحظات الموحد
- `ClientsImportDialog.tsx` - نافذة استيراد العملاء
- `ExcelParser.ts` - معالج ملفات Excel

### المكتبة المطلوبة
```bash
npm install xlsx  # لقراءة ملفات Excel
```

---

## خطوات التنفيذ

1. **تحديث قاعدة البيانات**
   - إضافة أعمدة جهة الاتصال
   - تحويل subscription_notes إلى customer_notes
   - تحديث سياسات RLS

2. **إنشاء مكون الملاحظات الموحد**
   - دعم جميع أنواع الملاحظات
   - فلترة حسب النوع
   - عرض اسم الموظف والتاريخ

3. **تحديث BasicInfoTab**
   - إضافة قسم جهة الاتصال المخولة
   - عرض جميع الملاحظات

4. **تحديث التبويبات الأخرى**
   - استخدام CustomerNotesSection في كل تبويب

5. **إنشاء ميزة استيراد Excel**
   - نموذج Excel للتحميل
   - واجهة رفع الملف
   - معاينة البيانات والتحقق
   - تنفيذ الاستيراد
