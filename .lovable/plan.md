

# تحليل وتصميم نظام إدارة علاقات العملاء (CRM) لمنصة ويبيان

---

## ملخص تنفيذي

بناءً على تحليل البنية الحالية للنظام، وجدتُ أن المنصة تمتلك أساسًا قويًا يمكن البناء عليه لإنشاء نظام CRM متكامل. الهدف هو توحيد جميع بيانات العملاء في مكان واحد مع الحفاظ على البساطة وقابلية التطوير.

---

## أولًا: تحليل الوضع الراهن

### الجداول الموجودة حاليًا:
| الجدول | الوصف | الحالة |
|--------|-------|--------|
| `client_organizations` | بيانات المؤسسات الأساسية والاشتراكات | موجود ✅ |
| `client_accounts` | حسابات مستخدمي العملاء | موجود ✅ |
| `support_tickets` | تذاكر الدعم الفني | موجود ✅ |
| `meeting_requests` | طلبات الاجتماعات | موجود ✅ |
| `conversations` | المحادثات | موجود ✅ |
| `subscription_requests` | طلبات تغيير الاشتراك | موجود ✅ |

### الفجوات المكتشفة:
1. **غياب حالة دورة حياة العميل (Lifecycle Stage)** - لا يوجد حقل يحدد مرحلة العميل
2. **غياب جدول الفواتير والمدفوعات** - لا يوجد نظام فوترة
3. **غياب سجل نشاط موحد للعميل** - النشاطات مبعثرة في جداول منفصلة
4. **غياب بيانات الأنظمة والروابط** - لا توجد طريقة لتخزين روابط لوحات التحكم
5. **نوع المؤسسة محدود** - أنواع المؤسسات الحالية مخصصة للجمعيات فقط

---

## ثانيًا: التصميم المعماري المقترح

### هيكل البيانات الجديد:

```text
┌─────────────────────────────────────────────────────────────────────┐
│                     نظام CRM لمنصة ويبيان                           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
┌───────────────┐         ┌─────────────────┐         ┌─────────────────┐
│ العميل (Org)  │◄────────│ جهات الاتصال   │         │ سجل النشاط      │
│               │         │ (Accounts)      │         │ (Timeline)      │
└───────┬───────┘         └─────────────────┘         └─────────────────┘
        │
        ├──────────────┬──────────────┬──────────────┬──────────────┐
        ▼              ▼              ▼              ▼              ▼
┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐
│ الاشتراك   │  │ الفواتير   │  │ التذاكر    │  │ الاجتماعات │  │ الأنظمة    │
│ والخطة    │  │ والمدفوعات│  │ والدعم     │  │            │  │ والحسابات  │
└────────────┘  └────────────┘  └────────────┘  └────────────┘  └────────────┘
```

---

## ثالثًا: تفاصيل التصميم

### 1. تعديلات جدول `client_organizations`:

**حقول جديدة مقترحة:**

| الحقل | النوع | الوصف |
|-------|------|-------|
| `customer_type` | enum | نوع العميل (اشتراكات/منصات مخصصة/خدمات) |
| `lifecycle_stage` | enum | مرحلة دورة الحياة |
| `auto_renewal` | boolean | التجديد التلقائي |
| `internal_notes` | text | ملاحظات داخلية |
| `primary_contact_id` | uuid | جهة الاتصال الرئيسية |
| `assigned_account_manager` | uuid | مدير الحساب المسؤول |
| `customer_value` | decimal | قيمة العميل المحسوبة |
| `tags` | text[] | وسوم للتصنيف |

**أنواع العملاء (customer_type):**
```sql
CREATE TYPE customer_type AS ENUM (
  'subscription',      -- عملاء اشتراكات ويبيان
  'custom_platform',   -- عملاء منصات مخصصة
  'services'           -- عملاء خدمات/مشاريع
);
```

**مراحل دورة الحياة (lifecycle_stage):**
```sql
CREATE TYPE customer_lifecycle_stage AS ENUM (
  'prospect',          -- عميل محتمل
  'negotiating',       -- قيد التعاقد
  'onboarding',        -- قيد التنفيذ
  'active',            -- نشط
  'suspended',         -- موقوف
  'churned'            -- منتهي الاشتراك
);
```

### 2. جدول جديد: `client_systems` (الأنظمة والحسابات)

```sql
CREATE TABLE client_systems (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id uuid REFERENCES client_organizations(id) ON DELETE CASCADE,
  system_type text NOT NULL,           -- 'website', 'portal', 'admin', 'api'
  name text NOT NULL,                  -- اسم النظام
  url text,                            -- رابط النظام
  username text,                       -- اسم المستخدم
  access_status text DEFAULT 'active', -- 'active', 'suspended', 'expired'
  notes text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

### 3. جدول جديد: `client_invoices` (الفواتير)

```sql
CREATE TABLE client_invoices (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id uuid REFERENCES client_organizations(id) ON DELETE CASCADE,
  invoice_number text UNIQUE NOT NULL,
  amount decimal(10,2) NOT NULL,
  currency text DEFAULT 'SAR',
  status text DEFAULT 'pending',       -- pending, paid, overdue, cancelled
  issue_date date NOT NULL,
  due_date date NOT NULL,
  paid_at timestamptz,
  description text,
  items jsonb,                         -- تفاصيل البنود
  notes text,
  created_by uuid,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

### 4. جدول جديد: `client_payments` (المدفوعات)

```sql
CREATE TABLE client_payments (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id uuid REFERENCES client_organizations(id) ON DELETE CASCADE,
  invoice_id uuid REFERENCES client_invoices(id),
  amount decimal(10,2) NOT NULL,
  payment_method text,                 -- bank_transfer, card, cash
  payment_date date NOT NULL,
  reference_number text,
  notes text,
  created_by uuid,
  created_at timestamptz DEFAULT now()
);
```

### 5. جدول جديد: `client_timeline` (سجل النشاط الموحد)

```sql
CREATE TABLE client_timeline (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id uuid REFERENCES client_organizations(id) ON DELETE CASCADE,
  event_type text NOT NULL,            -- أنواع الأحداث
  title text NOT NULL,
  description text,
  metadata jsonb,                      -- بيانات إضافية
  reference_type text,                 -- ticket, meeting, invoice, etc.
  reference_id uuid,
  performed_by uuid,
  performed_by_name text,
  created_at timestamptz DEFAULT now()
);

-- أنواع الأحداث:
-- 'created', 'stage_changed', 'ticket_opened', 'ticket_closed',
-- 'meeting_scheduled', 'meeting_completed', 'invoice_sent',
-- 'payment_received', 'note_added', 'contact_added', 'system_accessed'
```

---

## رابعًا: تصميم واجهة المستخدم (UX)

### صفحة ملف العميل الموحدة:

```text
┌─────────────────────────────────────────────────────────────────────┐
│  ◀ العودة      شركة ABC للتقنية                    [تعديل] [إجراءات▼]│
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  نوع: اشتراكات ويبيان                                 │
│  │  LOGO    │  الحالة: ● نشط        المرحلة: 🟢 عميل نشط            │
│  └──────────┘  المسؤول: أحمد محمد   آخر تفاعل: منذ 3 أيام          │
├─────────────────────────────────────────────────────────────────────┤
│ [البيانات] [الاشتراك] [الفواتير] [الأنظمة] [التذاكر] [الاجتماعات] [النشاط]│
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│     ┌────────────────────────────────────────────────────────┐     │
│     │           محتوى التبويب المحدد                        │     │
│     │                                                        │     │
│     │                                                        │     │
│     └────────────────────────────────────────────────────────┘     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### تفاصيل كل تبويب:

#### 1. البيانات الأساسية:
- معلومات المؤسسة (الاسم، النوع، السجل التجاري)
- بيانات التواصل (البريد، الهاتف، الموقع، المدينة)
- جهات الاتصال (مع تحديد الشخص الرئيسي)
- الملاحظات الداخلية (مخفية عن العميل)
- الوسوم والتصنيفات

#### 2. الاشتراك والخطة:
- نوع الباقة الحالية
- تواريخ البدء والانتهاء
- شريط تقدم الاشتراك المتبقي
- حالة التجديد التلقائي
- زر "طلب ترقية/تخفيض"
- سجل تغييرات الخطط

#### 3. الفواتير والمدفوعات:
- قائمة الفواتير مع الحالة (ملونة)
- إحصائيات سريعة (إجمالي المستحق، المدفوع)
- زر "إنشاء فاتورة جديدة"
- تنبيهات التأخير

#### 4. الأنظمة والحسابات:
- بطاقات للأنظمة المرتبطة
- روابط سريعة للوحات التحكم
- حالة الوصول (نشط/موقوف)
- زر "إعادة تعيين كلمة المرور"

#### 5. التذاكر والدعم:
- قائمة التذاكر مع الفلترة
- إحصائيات (مفتوحة/مغلقة)
- زر "فتح تذكرة جديدة"
- ربط مباشر بتفاصيل التذكرة

#### 6. الاجتماعات:
- الاجتماعات القادمة والسابقة
- ملاحظات كل اجتماع
- زر "جدولة اجتماع"

#### 7. سجل النشاط (Timeline):
- عرض زمني لكل الأحداث
- فلترة حسب النوع
- تفاصيل كل حدث

---

## خامسًا: تكامل نظام الصلاحيات

### تحديث مصفوفة الصلاحيات:

```typescript
// إضافات على RolePermissions
interface CRMPermissions {
  // CRM Permissions
  canViewCRM: boolean;
  canManageCustomerData: boolean;
  canManageInvoices: boolean;
  canViewInvoices: boolean;
  canManagePayments: boolean;
  canManageSystems: boolean;
  canViewTimeline: boolean;
  canChangeLifecycleStage: boolean;
}
```

### توزيع الصلاحيات:

| الصلاحية | المدير | المحرر | الدعم الفني | العميل |
|----------|--------|--------|-------------|--------|
| عرض CRM | ✅ | ❌ | 🔸 محدود | ❌ |
| تعديل بيانات العميل | ✅ | ❌ | ❌ | ❌ |
| إدارة الفواتير | ✅ | ❌ | ❌ | ❌ |
| عرض الفواتير | ✅ | ❌ | ❌ | ❌ |
| إدارة الأنظمة | ✅ | ❌ | ❌ | ❌ |
| عرض التذاكر | ✅ | ❌ | ✅ المسندة | ✅ الخاصة |
| تغيير مرحلة العميل | ✅ | ❌ | ❌ | ❌ |

---

## سادسًا: خارطة الطريق (Roadmap)

### المرحلة الأولى (4-6 أسابيع):

**الأولوية: إنشاء ملف العميل الأساسي**

1. **تعديل جدول المؤسسات:**
   - إضافة `customer_type` و `lifecycle_stage`
   - إضافة `internal_notes` و `tags`
   - إنشاء enum للأنواع والمراحل

2. **إنشاء صفحة ملف العميل الموحدة:**
   - تبويب البيانات الأساسية
   - تبويب الاشتراك
   - تبويب التذاكر (ربط موجود)
   - تبويب الاجتماعات (ربط موجود)

3. **إنشاء جدول Timeline:**
   - سجل الأحداث الموحد
   - Trigger لتسجيل تغييرات الحالة تلقائيًا

4. **تحديث صفحة قائمة العملاء:**
   - فلترة حسب المرحلة والنوع
   - عرض إحصائيات سريعة
   - رابط مباشر لملف العميل

### المرحلة الثانية (4-6 أسابيع):

**الأولوية: نظام الفوترة والتنبيهات**

1. **إنشاء نظام الفواتير:**
   - جدول الفواتير والمدفوعات
   - واجهة إنشاء/تعديل الفواتير
   - حالات الفواتير والتنبيهات

2. **نظام التنبيهات:**
   - تنبيه قبل انتهاء الاشتراك
   - تنبيه الفواتير المتأخرة
   - إشعارات للمدير

3. **جدول الأنظمة:**
   - ربط الأنظمة بالعميل
   - إعادة تعيين كلمات المرور
   - حالة الوصول

4. **التقارير الأساسية:**
   - إحصائيات العملاء
   - تقارير الفواتير

### المرحلة الثالثة (4-6 أسابيع):

**الأولوية: التحليلات المتقدمة**

1. **تحليلات العملاء:**
   - حساب قيمة العميل (CLV)
   - معدل الاحتفاظ (Retention Rate)
   - تحليل Churn

2. **لوحة تحكم CRM:**
   - Dashboard مخصص للمبيعات
   - رسوم بيانية للأداء
   - قمع المبيعات (Sales Funnel)

3. **التكاملات:**
   - تصدير التقارير
   - ربط مع أنظمة خارجية (مستقبلي)

---

## سابعًا: مكونات الواجهة المقترحة

### الملفات الجديدة المطلوبة:

```text
src/
├── pages/admin/
│   ├── crm/
│   │   ├── CustomerProfilePage.tsx    -- صفحة ملف العميل
│   │   ├── CustomerListPage.tsx       -- قائمة العملاء المحسنة
│   │   ├── InvoicesPage.tsx           -- إدارة الفواتير
│   │   └── CRMDashboardPage.tsx       -- لوحة تحكم CRM
│   │
├── components/crm/
│   ├── CustomerHeader.tsx             -- رأس ملف العميل
│   ├── CustomerTabs.tsx               -- تبويبات ملف العميل
│   ├── tabs/
│   │   ├── BasicInfoTab.tsx           -- البيانات الأساسية
│   │   ├── SubscriptionTab.tsx        -- الاشتراك
│   │   ├── InvoicesTab.tsx            -- الفواتير
│   │   ├── SystemsTab.tsx             -- الأنظمة
│   │   ├── TicketsTab.tsx             -- التذاكر
│   │   ├── MeetingsTab.tsx            -- الاجتماعات
│   │   └── TimelineTab.tsx            -- سجل النشاط
│   ├── LifecycleBadge.tsx             -- شارة مرحلة الحياة
│   ├── CustomerTypeBadge.tsx          -- شارة نوع العميل
│   ├── InvoiceForm.tsx                -- نموذج الفاتورة
│   └── TimelineItem.tsx               -- عنصر Timeline
```

---

## ثامنًا: اعتبارات تقنية

### الأمان:
- RLS على جميع الجداول الجديدة
- التحقق من صلاحية المستخدم قبل أي عملية
- إخفاء البيانات الحساسة (كلمات المرور)

### الأداء:
- Lazy loading للتبويبات
- Pagination لقوائم الفواتير والتذاكر
- Caching للبيانات المتكررة

### قابلية التوسع:
- استخدام jsonb للبيانات المرنة
- فصل الجداول حسب الوظيفة
- APIs موحدة قابلة للتوسع

---

## ملخص الخطة

| المرحلة | المدة | المخرجات الرئيسية |
|---------|-------|-------------------|
| الأولى | 4-6 أسابيع | ملف العميل + Timeline + تحسين القائمة |
| الثانية | 4-6 أسابيع | الفواتير + التنبيهات + الأنظمة |
| الثالثة | 4-6 أسابيع | التحليلات + لوحة CRM + التكاملات |

**إجمالي الوقت المقدر: 12-18 أسبوع**

